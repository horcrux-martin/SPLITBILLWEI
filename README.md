# SPLIT BILL-IN PLS
Split bill tanpa drama.

## Structure
- /client (React + Vite)
- /server (Node + Express + Google Vision)

## Run locally (requires Node)
### Server
cd server
npm install
# create server/.env and Google credentials
npm run dev

### Client
cd client
npm install
npm run dev
